@using Microsoft.AspNet.Identity
@using Microsoft.Owin.Security

@if (Request.IsAuthenticated)
{  
    @Html.ActionLink( "Gérer votre compte", "Manage", "Account", routeValues: null, htmlAttributes: new { title = "Administrer" })
       
}
